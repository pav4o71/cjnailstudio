[build]
  command = "npm run build"
  publish = ".next"

# Preview, branch, and unset-domain deploys stay non-indexable until ODR-024.
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
