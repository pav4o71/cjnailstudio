# Phase 0 requires no production secrets.
# Missing or invalid booking configuration must remain manual-handoff.
# Default production mode is the owner-approved Pavells embed (D-016).
BOOKING_MODE=embedded-widget
